finish option 1 : 
- change variable name so it wont be duplicate 
- auto generate ticket for specific vehicle type (not quite figure it out yet)
-traverse the list to look for the liscense plate to delete ()
    -> did we find the match?
        1.It's the only node in the list
        2.It's the first node in a list with multiple nodes
        3.It's the last node 
        4.It's in the middle of the list
        decrease the capcity count 
        delete } move to the next node } return false