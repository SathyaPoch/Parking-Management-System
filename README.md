algorithm folder is for writing algorithms. For example : merge sort , quick sort, or searching algorithm..

data strcuture folder is for tree , linked list..

To run the program(//could replace -std=c++17 with any version you desire.):
g++ -std=c++17 -Isrc src/main.cpp src/data_structure/linked_list.cpp -o parking_system && ./parking_system 
then :
./parking_system 